const LazyComponent = () => {
    return (
        <>
            <h1>Lazy Loaded Component</h1>
            <p>This component is loaded lazily</p>
        </>
    );
};

export default LazyComponent;
